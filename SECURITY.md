# 安全政策

## 支持范围

项目仍处于持续开发阶段，目前仅维护默认分支上的最新代码。旧提交、个人 Fork 和第三方打包版本不提供安全修复保证。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security > Advisories > Report a vulnerability** 私密报告功能。报告中建议包含：

- 受影响的版本或提交
- 可复现步骤和必要的最小样例
- 潜在影响与攻击前提
- 已知缓解方式（如有）

请不要在公开 Issue、讨论、日志或截图中披露漏洞细节、真实数据、密码、JWT、数据库文件或上传文件。维护者确认并修复问题后，再协商公开时间和披露内容。

## 部署责任

本仓库提供的是可二次开发的应用基础，不代表默认配置适合公网生产环境。部署者至少需要：

- 替换示例管理员密码和 JWT 密钥
- 使用 HTTPS、反向代理和最小端口暴露
- 根据实际域名限制 CORS 来源
- 限制数据库、上传目录和备份文件的访问权限
- 定期升级 Go、Node.js 和第三方依赖
- 建立数据库备份、恢复演练和日志留存策略
